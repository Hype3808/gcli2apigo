# Server Configuration
HOST=0.0.0.0
PORT=7860

# Authentication Configuration
# Choose one of the following strategies:

# Option 1: Universal password (recommended for simplicity)
# PASSWORD=your_universal_password
# When PASSWORD is set, it overrides GEMINI_AUTH_PASSWORD and GEMINI_API_KEY
# Both dashboard and API will use this password

# Option 2: Separate passwords for dashboard and API
GEMINI_AUTH_PASSWORD=123456              # Dashboard login only
GEMINI_API_KEY=123456                    # API requests only

# Note: If PASSWORD is set, GEMINI_AUTH_PASSWORD and GEMINI_API_KEY are ignored

# OAuth Credentials
OAUTH_CREDS_FOLDER=oauth_creds

# API Configuration
MAX_RETRY_ATTEMPTS=5

# Rate Limiting Configuration (for high RPS scenarios with shared IP)
# CREDENTIAL_RATE_LIMIT_RPS: Max requests per second per credential (default: 8)
# Lower values = more conservative (better for avoiding 429 errors)
# Higher values = more aggressive (better throughput but higher 429 risk)
# Example: With 10 credentials and RPS=8, max throughput = 80 RPS
CREDENTIAL_RATE_LIMIT_RPS=8

# Set to true to disable rate limiting (not recommended for shared IP scenarios)
# DISABLE_RATE_LIMITING=false

# Proxy Configuration (optional)
# HTTP_PROXY=http://proxy.example.com:8080
# HTTPS_PROXY=http://proxy.example.com:8080

# API Endpoints (optional, defaults shown)
# GEMINI_API_ENDPOINT=https://cloudcode-pa.googleapis.com
# GCP_RESOURCE_MANAGER_ENDPOINT=https://cloudresourcemanager.googleapis.com
# GCP_SERVICE_USAGE_ENDPOINT=https://serviceusage.googleapis.com
# OAUTH2_ENDPOINT=https://oauth2.googleapis.com
# GOOGLE_APIS_ENDPOINT=https://www.googleapis.com

# Localization
DEFAULT_LANGUAGE=zh

# Debug
DEBUG_LOGGING=false
